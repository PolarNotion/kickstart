# kickstart

This is a Rails 6 app.

## Prerequisites

* Ruby 2.7.0
* Postgres

* ImageMagick or libvips


## Getting Started

* clone this repo
* run `bundle install`
* run `rails db:setup`
* run `rails server`
* visit `//localhost:3000` in your browser
